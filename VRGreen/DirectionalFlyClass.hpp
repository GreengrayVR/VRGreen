#pragma once

class DirectionalFlyClass
{
public:
    static bool CanFly;
    bool SpeedHack;


    //void ToggleFly()
    //{
    //    GameObject gameObject = VRCPlayer.Instance.gameObject;

    //    CanFly = !CanFly;
    //    gameObject.GetComponent<CharacterController>().enabled = !CanFly;
    //    Console.WriteLine(string.Format("Fly is now {0}", CanFly));
    //}
private:

};